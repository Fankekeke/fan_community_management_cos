<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.cos.dao.MerchandiseMapper">

    <select id="queryPage" resultType="java.util.LinkedHashMap">
        SELECT
            *
        FROM
            merchandise
        WHERE
            1 = 1
        <if test="queryParam.name != null and queryParam.name != ''">
            AND name LIKE CONCAT('%',#{queryParam.name},'%')
        </if>
        <if test="queryParam.category != null and queryParam.category != ''">
            AND category LIKE CONCAT('%',#{queryParam.category},'%')
        </if>
        ORDER BY created_at DESC
    </select>
</mapper>
